# Runtime secrets

Create a file named `web_password` in this directory before starting Docker
Compose. It must contain only the Web administrator password and must not be
committed.

Recommended permissions are `0600`.
